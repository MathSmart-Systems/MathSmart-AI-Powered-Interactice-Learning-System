@AGENTS.md

Claude Code must treat the human live-acceptance gate in `AGENTS.md` as blocking: after implementation and automated verification, tell the user how to run or open the development application, ask them to perform live testing, request their explicit `LIVE ACCEPTANCE: PASS`, and stop. Do not begin CodeRabbit review or enable merge automation before that approval.

`AGENTS.md` and `CLAUDE.md` are frozen governance files. Do not modify either file unless the user explicitly authorizes the exact change and a human maintainer applies the required `agent-rules-change-approved` pull-request label.
